<nav class="nav" aria-label="Primary">
  <a class="nav__mark" href="/" aria-label="{{ site.couple.short }} — home">
    {% include "partials/monogram.njk" %}
  </a>
  <ul class="nav__links">
    {% for item in site.nav %}
      <li><a href="{{ item.url }}"{% if item.url == '/rsvp/' %} class="nav__cta"{% endif %}{% if item.url == page.url %} aria-current="page"{% endif %}>{{ item.label }}</a></li>
    {% endfor %}
  </ul>
</nav>
